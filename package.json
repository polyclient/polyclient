{
  "name": "polyclient",
  "private": true,
  "version": "0.1.0-alpha.9",
  "workspaces": ["gui"],
  "scripts": {
    "lint": "biome lint --write .",
    "format": "biome format --write . && prettier --write 'gui/**/*.svelte'",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "prettier": "^3.5.3",
    "prettier-plugin-svelte": "^3.3.3"
  }
}
